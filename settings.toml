# Non-secret configuration. Secrets arrive as PYTHON_APP_BASELINE_-prefixed environment variables.
# Select an environment with PYTHON_APP_BASELINE_ENV=production.

[default]
log_level = "INFO"

[production]
log_level = "WARNING"
